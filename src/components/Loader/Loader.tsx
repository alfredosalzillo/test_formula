import React from 'react';
import classes from './Loader.module.css';

/**
 * A simple circular loader.
 * Just use it.
 */
const Loader = () => (
  <div className={classes.root}>
    <div />
    <div />
    <div />
    <div />
  </div>
);

export default Loader;
